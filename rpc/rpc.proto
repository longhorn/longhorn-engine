syntax="proto3";

service LonghornLauncherService {
	rpc UpgradeEngine(Engine) returns (Empty) {}
}

message Engine {
	string binary = 1;
	string listen = 2;
	repeated string replicas = 3;
	repeated string enable_backends = 4;
}

message Empty {
}
